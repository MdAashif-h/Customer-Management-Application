"use client";
import Link from "next/link";
import { Palette, ShieldCheck, UserCircle, Menu } from "lucide-react";
import { useState } from "react";
import { DashboardSidebar } from "@/components/dashboard/dashboard-sidebar";
export function SettingsPage({ user }: { user: { name: string | null; email: string | null; role: string } }) { const [open, setOpen] = useState(false); return <div className="dashboard-page workspace-page"><DashboardSidebar mobileOpen={open} onClose={() => setOpen(false)} activeLabel="Settings" /><div className="dashboard-main"><header className="workspace-header"><button className="dashboard-menu-button" onClick={() => setOpen(true)} aria-label="Open navigation"><Menu size={18} /></button><div><p>Manage / Settings</p><h1>Settings</h1><span>Manage your Custora workspace preferences and account settings.</span></div></header><main className="settings-grid"><section className="dash-card settings-card"><div className="settings-card-heading"><UserCircle size={18} /><div><h2>Account</h2><p>Your personal workspace details.</p></div></div><div className="settings-detail"><span>Name</span><strong>{user.name ?? "Not set"}</strong></div><div className="settings-detail"><span>Email</span><strong>{user.email ?? "Not set"}</strong></div><div className="settings-detail"><span>Role</span><strong>{user.role}</strong></div><Link href="/profile" className="settings-link">Manage profile</Link></section><section className="dash-card settings-card"><div className="settings-card-heading"><Palette size={18} /><div><h2>Appearance</h2><p>Control the visual experience.</p></div></div><div className="theme-choice"><span className="theme-swatch" /><div><strong>Dark theme</strong><small>Custora&apos;s focused default experience</small></div><span className="theme-check">Active</span></div></section><section className="dash-card settings-card"><div className="settings-card-heading"><ShieldCheck size={18} /><div><h2>Security</h2><p>Authentication and session controls.</p></div></div><p className="settings-copy">Password and session security are managed through the current Auth.js authentication system.</p></section></main></div></div>; }